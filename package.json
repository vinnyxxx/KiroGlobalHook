{
  "name": "kiro-hooks-init",
  "displayName": "Kiro Global Hook",
  "description": "Auto-detect and initialize Kiro hooks from a configurable template directory when opening a workspace",
  "version": "0.8.0",
  "publisher": "VinnyXu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vinnyxxx/KiroGlobalHook.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "extensionKind": ["ui"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "kiroHooksInit.clearAllHooks",
        "title": "Kiro Hooks: Clear All Hooks"
      }
    ],
    "configuration": {
      "title": "Kiro Hooks Init",
      "properties": {
        "kiroHooksInit.templateDir": {
          "type": "string",
          "default": "",
          "scope": "application",
          "description": "Absolute path to a directory containing hook template files. These will be copied to .kiro/hooks/ in new workspaces."
        },
        "kiroHooksInit.fileExtensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [".kiro.hook", ".json", ".sh"],
          "scope": "application",
          "description": "File extensions to sync from the template directory to .kiro/hooks/. Files matching any of these suffixes will be copied."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "package": "npx @vscode/vsce package --no-dependencies --allow-missing-repository"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "@vscode/vsce": "^3.2.0"
  }
}
